import styled from "styled-components"
import styles from "../ToolsSection/styles"

const Styles = styled.div`
    
    width: 100%;

    display: flex;

    gap: 0.5rem;

    span{
        display: inline-block;

        padding: 0.5rem;

        font-size: 0.8em;
        font-weight: 700;
        line-height: 1;

        text-align: center;

        white-space: nowrap;

        background: var(--purple-100);

        border-radius: 0.2rem;
    }
`

export default Styles